import { SwipeableDrawer as MuiSwipeableDrawer } from '@mui/material';
import { styled } from '@mui/system';

/**
 * [atoms] MuiのSwipeableDrawerにプロジェクト用themeを適用したコンポーネント
 * @param props - MuiSwipeableDrawer
 */
export const SwipeableDrawer = styled(MuiSwipeableDrawer)(() => ({}));
